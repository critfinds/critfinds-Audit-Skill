# Findings

This directory holds audit reports from previous `/critfindsaudit` runs, saved as `critfinds-audit-{project-name}-{timestamp}.md` when `--file-output` flag is used.

Drop any external audit reports here for cross-referencing.
